{
  "name": "@amitgupta-portfolio/source",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "nx dev frontend",
    "dev:admin": "nx dev admin",
    "dev:all": "nx run-many -t dev --parallel",
    "seed": "npm run seed -w admin",
    "build": "nx run-many -t build",
    "start": "nx start frontend",
    "lint": "nx run-many -t lint",
    "test": "nx run-many -t test",
    "graph": "nx graph"
  },
  "devDependencies": {
    "nx": "^22.6.5"
  },
  "dependencies": {
    "next": "16.3.0"
  }
}
