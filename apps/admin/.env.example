# Postgres connection string
DATABASE_URL=postgres://USER@localhost:5432/amitgupta_portfolio
# Long random string, e.g. `openssl rand -hex 32`
PAYLOAD_SECRET=
# Public URL of this admin app (used to build absolute media URLs)
SERVER_URL=http://localhost:3001
# Frontend to notify when content changes; the secret must match the frontend's REVALIDATE_SECRET
FRONTEND_URL=http://localhost:3000
REVALIDATE_SECRET=
